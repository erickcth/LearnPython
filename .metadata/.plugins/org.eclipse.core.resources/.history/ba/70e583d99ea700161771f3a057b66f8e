'''
Created on 10 Nov 2016

@author: Erick
'''

import array;

def checkPrime(s, primes):
    for prime in primes:
        if s % prime == 0:
            return False
        
    return True


def isPrime(num):
    primes = array.array('0')
    
    s = 2
    while s < num:
        result = checkPrime(s, primes);
        if result :
            primes.append(s)
        s = s + 1
    
    isPrime = False;
    
    print(isPrime);


if __name__ == '__main__':
    isPrime(100);